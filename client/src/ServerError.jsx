import styles from '../styles/serverError.module.sass';

const ServerError = () => {
    return (
        <div className={styles.serverError}>
            Server Error
        </div>
    );
};

export default ServerError;
